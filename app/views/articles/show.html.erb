<div class="container">
  <div class="title">
    <h1><%= @article.title %></h1>
    <%= link_to "Delete",
        article_path(@article),
        data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  </div>
</div>
